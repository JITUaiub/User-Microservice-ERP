#management:
# endpoints:
#  web:
#   exposure:
#    include: hystrix.stream
#   base-path: /

eureka:
 client:
  registerWithEureka: true
  fetchRegistry: true
  serviceUrl:
   defaultZone: http://localhost:8302/eureka/
  instance:
   hostname: localhost